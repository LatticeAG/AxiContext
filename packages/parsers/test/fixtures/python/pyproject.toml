[project]
name = "minimal-python"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "psycopg[binary]>=3.2"
]

[project.optional-dependencies]
dev = [
  "pytest>=8"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
