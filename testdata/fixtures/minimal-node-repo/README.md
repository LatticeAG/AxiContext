# Minimal Node Fixture

This fixture repository is used to validate Git adapter ingestion.

## Usage

Run `axictx sync` and assert that generated context includes architecture, tooling, and provenance sections.
