services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: axictx_fixture
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
